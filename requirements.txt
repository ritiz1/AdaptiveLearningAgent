# Adaptive AI Learning System — V1 backend
# Stack: FastAPI + LangGraph + LangChain + PostgreSQL + LangSmith

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# Agent orchestration & LLM
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langsmith>=0.1.0

# Free / low-cost model providers
langchain-groq>=0.2.0          # Groq free tier (Llama, Mixtral, Gemma)
langchain-google-genai>=2.0.0  # Gemini free tier
langchain-ollama>=0.2.0        # Local free models via Ollama
langchain-openai>=0.2.0        # OpenAI-compatible APIs (OpenRouter, etc.)

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg[binary,pool]>=3.2.0
asyncpg>=0.30.0

# Config & HTTP
python-dotenv>=1.0.1
httpx>=0.28.0
