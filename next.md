# Next Session — Finish the First Phase 2 Database Slice (2 Hours)

## Current checkpoint

### Completed

- [x] PostgreSQL 16 container is running in Docker
- [x] Persistent Docker volume is attached
- [x] `DATABASE_URL` and `GROQ_API_KEY` are configured
- [x] SQLAlchemy database engine and session factory exist
- [x] SQLAlchemy declarative `Base` exists
- [x] `learner_profiles` model exists
- [x] Initial `user_concept_states` model exists
- [x] Both models are registered in `Base.metadata`
- [x] Alembic is initialized and connected to `Base.metadata`
- [x] Initial migration was autogenerated

### Important current state

The generated migration has **not** been applied. This is correct: the
`UserConceptState` model must be fixed first, then the unapplied migration
must be regenerated.

Current migration revision:

```text
582e957b37df_add_learner_profiles_and_concept_state.py
```

Do not run `alembic upgrade head` with that migration.

---

## Goal for the next 2 hours

Correct the two-table schema, apply the first migration, seed the learner
profiles, and prove that PostgreSQL rows can be converted into existing
domain objects.

Do not modify the LangGraph workflow, AI nodes, prompts, or tools.

---

## First hour — Correct and apply the schema

### 1. Fix `concept_state.py` (20 minutes)

File:

```text
app/infrastructure/db/models/concept_state.py
```

Required corrections:

- [ ] Remove the unused `NoneIsAllowedError` import
- [ ] Use one consistent column name: `confidence`
- [ ] Add `evidence_count` as a non-negative integer
- [ ] Make `last_evidence_at` a nullable timezone-aware `DateTime`
- [ ] Add `misconception_tags` as a non-null `JSONB` list
- [ ] Keep `updated_at` as a timezone-aware `DateTime`
- [ ] Keep `(user_id, concept_id)` as the composite primary key
- [ ] Ensure all check constraints reference real column names

Important behavior:

```text
No user_concept_states row = mastery is unknown
Existing row with mastery 0.0 = observed low mastery
```

### 2. Regenerate the unapplied migration (15 minutes)

Because the current migration was never applied, delete only:

```text
alembic/versions/582e957b37df_add_learner_profiles_and_concept_state.py
```

Then generate it again:

```powershell
python -m alembic revision --autogenerate -m "add learner profiles and concept state"
```

### 3. Inspect the generated migration (10 minutes)

Confirm it creates:

```text
learner_profiles
user_concept_states
```

Confirm `user_concept_states` contains:

- `user_id`
- `concept_id`
- `mastery_estimate`
- `confidence`
- `evidence_count`
- `last_evidence_at`
- `misconception_tags`
- `updated_at`

Confirm every check constraint references a column that exists.

### 4. Apply and verify the migration (15 minutes)

Apply:

```powershell
python -m alembic upgrade head
```

Verify Alembic state:

```powershell
python -m alembic current
```

Verify tables:

```powershell
docker exec adaptive-learning-postgres psql -U postgres -d adaptive_learning -c "\dt"
```

Expected tables:

```text
alembic_version
learner_profiles
user_concept_states
```

---

## Second hour — Seed and read learner data

### 5. Create a learner seed script (20 minutes)

Create:

```text
scripts/seed_db.py
```

Seed only:

- [ ] `user-a` learner profile
- [ ] `user-b` learner profile

Copy their profile values from:

```text
app/services/learner_service/hardcoded.py
```

Do not seed concepts or prerequisite edges yet because their database tables
do not exist.

The seed script should be idempotent: running it twice must not create
duplicate profiles.

### 6. Create the learner repository (25 minutes)

Create:

```text
app/infrastructure/db/repositories/
├── __init__.py
└── learner_repository.py
```

Implement:

```text
get_profile(user_id) -> LearningProfile | None
get_mastery(user_id, concept_id) -> MasterySnapshot
```

Rules:

- Repository code owns SQLAlchemy queries
- Repository methods return domain objects, not ORM rows
- A missing concept-state row returns `MasterySnapshot(is_unknown=True)`
- Nodes and tools must not query SQLAlchemy models directly

### 7. Verify persistence (15 minutes)

Test:

- [ ] `get_profile("user-a")` returns the expected example-first profile
- [ ] `get_profile("user-b")` returns the expected top-down profile
- [ ] Unknown user returns `None`
- [ ] Missing mastery row returns `is_unknown=True`
- [ ] Stop and restart the PostgreSQL container
- [ ] Verify the profiles still exist after restart

Useful Docker commands:

```powershell
docker stop adaptive-learning-postgres
docker start adaptive-learning-postgres
```

Finally, verify the existing hardcoded application path still works:

```powershell
python smoke_test_llm.py
```

---

## End-of-session success criteria

- [ ] Corrected migration is applied successfully
- [ ] Alembic reports the latest revision
- [ ] Both database tables exist
- [ ] `user-a` and `user-b` are stored in PostgreSQL
- [ ] Repository returns domain models
- [ ] Missing mastery is represented as unknown
- [ ] Data survives a container restart
- [ ] Existing smoke test still passes

---

## Save for a later session

- PostgreSQL-backed service classes
- Switching `tool_context.py` from hardcoded to PostgreSQL services
- `concepts` and `concept_edges` tables
- `learning_evidence` and `sessions` tables
- LangGraph PostgreSQL checkpointer
- Concept resolution (Phase 3)
- Evaluator and mastery-update engine (Phase 5)
- Changes to graph nodes or routing

## References

- Architecture: `adaptive_ai_learning_system_v1_2_architecture.pdf`
- Current models: `app/infrastructure/db/models/`
- Hardcoded learner profiles: `app/services/learner_service/hardcoded.py`
- Domain models: `app/domain/learner/` and `app/domain/mastery/`
